import React from 'react';

const Modelo03 = ({ buttonText }: any) => {
    return (
        <div>
            <button style={{ padding: '10px 20px', borderRadius: '5px' }}>
                {buttonText}
            </button>
        </div>
    );
};

export default Modelo03;
